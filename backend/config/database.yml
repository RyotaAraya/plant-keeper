default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  url: <%= ENV.fetch("DATABASE_URL", "postgres://localhost/manage_development") %>

test:
  <<: *default
  database: manage_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
